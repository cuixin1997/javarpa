server:
  port: 8080

spring:
  application:
    name: rpa-server
  datasource:
    # 跨机/公网部署时置 MYSQL_SSL=true 加密链路
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/rpa?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=${MYSQL_SSL:false}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:root123456}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      # 与 docker-compose 中 redis requirepass 保持一致；独立裸跑 redis 时置空
      password: ${REDIS_PASSWORD:rpa_redis_dev}
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 55MB
  jackson:
    default-property-inclusion: non_null

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  global-config:
    banner: false

rpa:
  jwt-secret: ${RPA_JWT_SECRET:change-me-in-production-0123456789abcdef}
  jwt-expire-hours: 24
  # 调试期默认放行所有来源；生产必须用 RPA_CORS_ORIGINS 收紧为白名单
  cors-origins: ${RPA_CORS_ORIGINS:*}
  upload-dir: ${RPA_UPLOAD_DIR:./data/scripts}
  heartbeat-timeout-seconds: 90
  offline-scan-seconds: 30
  log-retention-days: 30
  # Redis 故障降级期间登录失败内存计数的清理周期
  login-state-clean-seconds: 300

springdoc:
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.rpa.server: info
